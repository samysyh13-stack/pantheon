# Copy to .env.local and fill values for local development.
# All entries are optional; unset means the feature is disabled at runtime.

# Sentry browser SDK DSN (optional)
VITE_SENTRY_DSN=

# Plausible analytics domain (optional)
VITE_PLAUSIBLE_DOMAIN=
